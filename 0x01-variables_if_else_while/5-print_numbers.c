#include <stdio.h>

/**
 *main- Program that print the numbers of base 10
 *Return: exit succesful
 */

int main(void)
{
int i = 0;
for (; i < 10; i++)
putchar(i + '0');
putchar('\n');

return (0);
}
